<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ticketac - Results</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <link rel='stylesheet' href='/stylesheets/style.css' />

</head>

<body>
    <%- include('navbar'); %>
        <div class="container-fluid result">
            <div class="col-12 text-center">
                <div class="col-8 mt-5 mb-3">
                    <h4>Tickets available on <%=date%> :</h4>
                </div>
            </div>
            <div class="col-8 align-items-center">
                <ul class="list-group"></ul>
                <% for (var i=0; i <newJourney.length; i++){%>
                    <li class="list-group-item d-flex justify-content-between align-items-center mb-2 p-4">
                        <div class="cities fw-bold">
                            <%=newJourney[i].departure%> / <%=newJourney[i].arrival%>
                        </div>
                        <div class="info">Train departure at: <%=newJourney[i].departureTime%>
                        </div>
                        <div class="cities fw-bold">Prix <%=newJourney[i].price%>€</div>
                        <a href="trajet?id=<%=newJourney[i].id%>" type="button" class="btn btn-danger">OK</a>
                    </li>

                    <% } %>


                        </ul>
            </div>
        </div>
</body>
<%- include('footer'); %>
</html>